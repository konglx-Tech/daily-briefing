name: 午间快报

on:
  schedule:
    # 北京时间周一至周五 11:45 (UTC 03:45)
    - cron: '45 3 * * 1-5'
  workflow_dispatch:

jobs:
  midday-briefing:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Midday Briefing
        env:
          SEND_KEY: ${{ secrets.SEND_KEY }}
        run: python stock_briefing.py --mode midday
